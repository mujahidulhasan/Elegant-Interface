{
  "framework": "vite",
  "buildCommand": "BASE_PATH=/ pnpm build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
