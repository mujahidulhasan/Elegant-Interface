{
  "framework": "vite",
  "buildCommand": "pnpm --filter @workspace/clipvlt build",
  "outputDirectory": "artifacts/clipvlt/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
