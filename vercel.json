{
  "framework": "vite",
  "buildCommand": "cd artifacts/clipvlt && BASE_PATH=/ pnpm build",
  "outputDirectory": "artifacts/clipvlt/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
